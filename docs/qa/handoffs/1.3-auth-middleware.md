# QA Handoff — Story 1.3: Implement API Authentication Middleware

Scope
- Verify static Bearer auth middleware behavior and public-path bypass.
- Validate readiness per PRD Epic 1.3 ACs and story Testing section.

Branch & Paths
- Branch: `feat/story-1.3-auth-middleware`
- Story file: `docs/stories/1.3.implement-api-authentication-middleware.md`
- API root: `dream-central-storage-api/`

Setup
```
cd dream-central-storage-api
python -m venv .venv && source .venv/bin/activate
pip install -e '.[dev]'
export AUTH_BEARER_TOKEN='secret-token'
```

Run
- Unit tests: `pytest -m "not integration" -q` (expected: pass)
- Lint/format: `ruff check .` and `black --check .` (expected: clean)
- Manual API check:
  - `uvicorn app.main:app --reload`
  - Public: `curl -i localhost:8000/health`, `curl -i localhost:8000/storage/health`
  - Protected: `curl -i localhost:8000/api/v1/private/ping` → 401; with wrong token → 403; with correct token → 200

Acceptance Criteria Coverage
- AC1: Middleware validates `Authorization: Bearer <token>` → implemented in `app/middleware/auth.py`.
- AC2: Protected endpoints return 401/403 appropriately → validated by unit tests and `/api/v1/private/ping` behavior.
- AC3: Extensible for RBAC (Depends + context) → `request.state.auth` context; `app/api/v1/deps.py#get_current_user` ready.

Artifacts
- Middleware: `app/middleware/auth.py`
- Config: `app/core/config.py` (`load_auth_config`, env `AUTH_BEARER_TOKEN`)
- Protected route stub: `app/api/v1/endpoints/private.py` (for E2E verification)
- Tests: `app/tests/unit/test_auth_middleware.py`, `app/tests/unit/test_private_route_auth.py`
- README updates: Authentication section with curl examples

Gate File Location
- Create gate at: `docs/qa/gates/1.3-implement-api-authentication-middleware.yml`

Notes
- No production endpoints were modified; stub route is for verification only.
- MinIO is not required for this story's QA; storage health remains public.

