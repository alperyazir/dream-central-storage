# Story 1.4: Upload Application Build

## Status
Draft

## Story
As an administrator, I want to upload a FlowBook application build via a secure API endpoint, so that it can be stored and versioned in the central storage.

## Acceptance Criteria
1. A secure `POST /api/v1/apps` endpoint is created that requires authentication.
2. The endpoint accepts a file upload, a `version` tag, and a `platform` value in {`linux`, `macos`, `windows`}.
3. Upon successful upload, the file is stored in the S3 bucket under a structured path `apps/{platform}/{version}/flowbook.zip` (lowercase segments).
4. A success response is returned with `version`, `platform`, and `location` of the stored file.

## Tasks / Subtasks
- [ ] API contract and validation
  - [ ] Define `POST /api/v1/apps` in `app/api/v1/endpoints/assets.py` (create file if missing)
  - [ ] Require auth via middleware + `Depends(get_current_user)`; return 401/403 accordingly
  - [ ] Accept multipart form with fields: `file: UploadFile`, `version: str`, `platform: str`
  - [ ] Validate `version` (non-empty; consider semver pattern)
  - [ ] Validate `platform` against enum {linux, macos, windows} (case-insensitive → normalize to lowercase)
  - [ ] Ensure `file.filename` is present
- [ ] Storage path and upload logic
  - [ ] Compute object path: `apps/{platform}/{version}/flowbook.zip` (use uploaded filename only if we later decide to support custom names)
  - [ ] Add helper in `app/services/storage_service.py` (or existing storage module) to stream upload to MinIO/S3
  - [ ] Use centralized S3 config from `app/core/config.py`; bucket from env (`S3_BUCKET`/`MINIO_BUCKET`)
  - [ ] On success, return JSON with `version`, `platform`, `location` (e.g., `s3://{bucket}/apps/{platform}/{version}/flowbook.zip`)
- [ ] Error handling and responses
  - [ ] On missing/invalid inputs → `422`
  - [ ] On auth missing/invalid → `401/403`
  - [ ] On storage error → log and return `500` with safe message
- [ ] Wire router
  - [ ] Add `assets` router under prefix `/api/v1/apps` and include in `app.main`
- [ ] Documentation
  - [ ] Update `dream-central-storage-api/README.md` with `POST /api/v1/apps` example (curl) including `Authorization` header
  - [ ] Document required env vars and sample response
  - [ ] Document `platform` values (linux, macos, windows) and path convention `apps/{platform}/{version}/flowbook.zip`
- [ ] Tests
  - [ ] Unit: happy path with auth (mock storage), 401/403 without/with wrong token, 422 on missing version/platform
  - [ ] Integration: with MinIO running, upload a zip and verify object at `apps/{platform}/{version}/flowbook.zip`

## Dev Notes
- Authentication and protection
  - Protected endpoints MUST enforce authentication using FastAPI’s dependency system and middleware.
  - Public endpoints remain `/health` and `/storage/health`.
  - [Source: docs/architecture/security.md#authentication-authorization]
- Source layout and components
  - Place endpoint under `app/api/v1/endpoints/assets.py` and include router from `app/main.py`.
  - Follow structure from the source tree for routers and services.
  - [Source: docs/architecture/source-tree.md]
  - [Source: docs/architecture/components.md#1-api-routers]
  - [Source: docs/architecture/components.md#4-storage-client]
- Storage and configuration
  - Use existing S3 config (`app/core/config.py`) and MinIO client helper; do not hardcode secrets.
  - Persist object to bucket path `apps/{version}/flowbook.zip`.
  - [Source: docs/architecture/tech-stack.md]
  - [Source: docs/architecture/coding-standards.md#core-standards]
- Error handling
  - Return clear HTTP statuses; log warnings/errors via standard logger.
  - [Source: docs/architecture/error-handling-strategy.md]

### Edge Cases & Assumptions
- `version` is required and non-empty; future stories may enforce stricter semver.
- `platform` must be one of {linux, macos, windows}; case-insensitive inputs are normalized to lowercase.
- If the object already exists, return success with idempotent overwrite or `409 Conflict` based on chosen policy (document chosen behavior in README; default: overwrite for MVP).
- File type is expected to be a zip; content-type/extension enforcement can be advisory in MVP and stricter later.
- Large uploads: streaming upload via `UploadFile` to avoid loading into memory.

### Testing
- Unit tests (pytest) under `app/tests/unit/`:
  - Authorized upload returns 201 with JSON containing `version` and `location`
  - Unauthorized (no header) → 401; wrong token → 403
  - Missing `version` or file → 422
- Integration tests under `app/tests/integration/` (requires MinIO):
  - Upload a small zip to `apps/{version}/flowbook.zip`, then verify existence via MinIO client
- Coverage target: 80%+ per test strategy.
  - [Source: docs/architecture/test-strategy-and-standards.md]

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2025-09-16 | v0 | Initial draft created from PRD Epic 1.4 | Scrum Master |

## Dev Agent Record
### Agent Model Used
TBD by dev-agent
### Debug Log References
TBD by dev-agent
### Completion Notes List
TBD by dev-agent
### File List
TBD by dev-agent

## QA Results
TBD by QA Agent
